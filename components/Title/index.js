import React from "react";
import { StyleSheet, Text } from "react-native";

const Index = () => {
  return <Text style={styles.title}>아아아</Text>;
};

const styles = StyleSheet.create({
  title: {
    fontSize: 40,
    fontWeight: "600",
    color: "black",
    margin: 30,
  },
});

export default Index;
